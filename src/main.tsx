import React from "react";
import ReactDOM from "react-dom/client";
import DecisionLog from "./DecisionLogApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <DecisionLog />
  </React.StrictMode>
);
